**Unreleased**

* Use literal PowerShell path arguments for copy and delete file actions.
* Default WinRM assets to certificate-validated HTTPS connections.
* Reject oversized AppLocker XML and documents containing type declarations.
* Report nonzero run command and run script exit codes as action failures.
